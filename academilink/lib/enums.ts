export enum PreferredTeachingMethods {
  זום = "ZOOM",
  פרונטלי = "FRONTAL",
  שניהם = "BOTH",
}

export enum PreferredTeachingMethodsReverse {
  ZOOM = "זום",
  FRONTAL = "פרונטלי",
  BOTH = "זום / פרונטלי",
}
