"use client";
import type { getAllSessionsForTutorType } from "@/actions/StudentSession";

const TutorSessions = ({
  sessionRequests,
}: {
  sessionRequests: getAllSessionsForTutorType;
}) => {
  return <div>TutorSessions</div>;
};

export default TutorSessions;
